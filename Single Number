class Solution {
    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            // XOR each number with the result
            result ^= num;
        }
        return result;
    }
}
